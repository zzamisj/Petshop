const nameElement = document.getElementById("name");
const introElement = document.getElementById("intro");

if (nameElement && introElement) {
  nameElement.textContent = "Jomer Amistoso";
  introElement.textContent = "QA Tester";
}